- name: install docker
  yum: name=docker state=present
  
 - name: make sure required PyPi packages are installed
  pip:
    name: '{{ item }}'
    state: present
  with_items:
    - docker-py>=1.10.6
    - six>=1.10.0